server:
  port: 10013
spring:
  application:
    name: config-service-dev
eureka:
  client:
    service-url:
      defaultZone=http: //127.0.0.1:8761/eureka/
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true
my:
  address:
    string: mark-star-world
